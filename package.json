{
  "name": "cafe-ordering-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "migrate": "cd backend && npm install && npm run migrate",
    "migrate:backend": "cd backend && npm run migrate",
    "install:backend": "cd backend && npm install"
  }
}
